namespace Unity.VersionControl.Git
{
    public enum GitResetMode
    {
        /// <summary>
        /// Defaults to mixed
        /// </summary>
        NonSpecified,
        Soft,
        Keep,
        Mixed,
        Hard
    }
}
